<ApiRequest xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xs="http://www.w3.org/2001/XMLSchema">
    <ApiKey>{{ apiKey }}</ApiKey>
    <MultiData>
        {% for Subscriber in rows %}
        <Subscriber>
            <Mode>{{ Subscriber.Mode }}</Mode>
            <Force>{{ Subscriber.Force }}</Force>
            <ListId>{{ Subscriber.ListId }}</ListId>
            <Email>{{ Subscriber.Email }}</Email>
            <Firstname>{{ Subscriber.Firstname }}</Firstname>
            <Lastname>{{ Subscriber.Lastname }}</Lastname>
            <TrackingCode>{{ Subscriber.TrackingCode }}</TrackingCode>
            <Vendor>{{ Subscriber.Vendor }}</Vendor>
            <Ip>{{ Subscriber.Ip }}</Ip>
            {% if Subscriber.Properties is defined and Subscriber.Properties %}
            <Properties>
                {% for Property in Subscriber.Properties %}
                <Property>
                    <Id>{{ Property.Id }}</Id>
                    <Value xsi:type="xs:string">{{ Property.value }}</Value> {# type to define #}
                </Property>
            </Properties>
            {% endif %}
        </Subscriber>
    </MultiData>
</ApiRequest>